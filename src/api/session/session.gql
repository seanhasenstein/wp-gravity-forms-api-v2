type Session {
  id: Int!
  title: String!
  category: String!
  level: String!
  date: String!
  time: String!
  location: String!
  year: String!
  crewDeal: Boolean!
  price: Int!
  officials: [Registration]!
  totalAttending: Int!
}

extend type Query {
  singleSession(id: ID!): Session!
  allSessions: [Session]!
}
